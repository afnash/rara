'use client';
import Link from'next/link';import{PawPrint,ArrowRight}from'lucide-react';import{createClient}from'@/lib/supabase/client';import{useRouter}from'next/navigation';import{useState}from'react';
export default function AuthForm({mode,defaultRole='parent'}:{mode:'login'|'signup',defaultRole?:'parent'|'sitter'}){const router=useRouter();const[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget),email=String(form.get('email')),password=String(form.get('password')),supabase=createClient();if(mode==='signup'){const role=String(form.get('role'));const{error}=await supabase.auth.signUp({email,password,options:{data:{full_name:String(form.get('name')),role}}});if(error)setError(error.message);else router.push('/login?created=1')}else{const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push('/dashboard')}setBusy(false)}return <main className="auth"><Link href="/" className="brand"><span><PawPrint/></span>RaRa</Link><form onSubmit={submit}><p className="eyebrow">{mode==='login'?'WELCOME BACK':'JOIN OUR COMMUNITY'}</p><h1>{mode==='login'?'Sign in to RaRa':'Create your account'}</h1>{mode==='signup'&&<><label>Full name<input name="name" required placeholder="Your full name"/></label><label>I am joining as<select name="role" defaultValue={defaultRole}><option value="parent">Pet parent</option><option value="sitter">Pet sitter</option></select></label></>}<label>Email<input type="email" name="email" required placeholder="you@example.com"/></label><label>Password<input type="password" name="password" minLength={8} required placeholder="At least 8 characters"/></label>{error&&<p className="error">{error}</p>}<button className="button" disabled={busy}>{busy?'Please wait...':mode==='login'?'Sign in':'Create account'}<ArrowRight/></button><p>{mode==='login'?<>New here? <Link href="/signup">Create an account</Link></>:<>Already have an account? <Link href="/login">Sign in</Link></>}</p></form></main>}
